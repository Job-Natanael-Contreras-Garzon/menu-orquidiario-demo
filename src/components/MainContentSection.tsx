'use client';

// ESTE COMPONENTE ESTÁ DESHABILITADO
// Contenía una versión duplicada de la sección de Ingredientes que no soportaba internacionalización (i18n)
// En su lugar, se utiliza el componente IngredientesSection.tsx que sí soporta cambio de idiomas

export function MainContentSection() {
  console.warn('MainContentSection está deshabilitado. Usar IngredientesSection en su lugar.');
  return null; // No renderizar nada
}
